Common properties can be stored in the protoype classes. <br>
to console every properties in an object, we can use =>
*for (var prop in chevy) {* <br>
....*console.log(prop: + " : " + chevy[prop]);* <br>
*}*

